{
  "scripts": {
    "dev": "live-server src --port=4500 --host=localhost"
  },
  "dependencies": {
    "live-server": "^1.2.2"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.6.5"
  }
}
